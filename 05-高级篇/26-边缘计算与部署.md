# 第二十六章：边缘计算与部署

> 掌握模型压缩、量化、边缘设备部署等技术

---

## 26.1 模型压缩

### 26.1.1 剪枝

**剪枝**移除不重要的权重。

```python
import torch.nn.utils.prune as prune

# 随机剪枝
prune.random_unstructured(model.conv1, name="weight", amount=0.3)

# 结构化剪枝
prune.ln_structured(model.conv1, name="weight", amount=0.3, n=2)
```

### 26.1.2 量化

**量化**降低精度减少模型大小。

```python
import torch

# 动态量化
model_quantized = torch.quantization.quantize_dynamic(
    model, {torch.nn.Linear}, dtype=torch.qint8
)

# 静态量化
model.qconfig = torch.quantization.get_default_qconfig('fbgemm')
torch.quantization.prepare(model, inplace=True)
torch.quantization.convert(model, inplace=True)
```

### 26.1.3 知识蒸馏

**蒸馏**大模型教小模型。

```python
class DistillationLoss(nn.Module):
    def __init__(self, temperature=4.0):
        super().__init__()
        self.temperature = temperature
        self.kl_div = nn.KLDivLoss()
    
    def forward(self, student_logits, teacher_logits):
        student_log_probs = nn.functional.log_softmax(
            student_logits / self.temperature, dim=1
        )
        teacher_probs = nn.functional.softmax(
            teacher_logits / self.temperature, dim=1
        )
        return self.kl_div(student_log_probs, teacher_probs)
```

---

## 26.2 ONNX

### 26.2.1 导出 ONNX

```python
import torch

# 导出
dummy_input = torch.randn(1, 3, 224, 224)
torch.onnx.export(
    model,
    dummy_input,
    "model.onnx",
    export_params=True,
    opset_version=11,
    input_names=['input'],
    output_names=['output']
)
```

### 26.2.2 ONNX Runtime 推理

```python
import onnxruntime as ort
import numpy as np

# 加载模型
session = ort.InferenceSession("model.onnx")

# 推理
input_name = session.get_inputs()[0].name
output = session.run(None, {input_name: input_data})
```

---

## 26.3 TensorRT

### 26.3.1 部署流程

```python
import tensorrt as trt

# 创建引擎
logger = trt.Logger(trt.Logger.WARNING)
builder = trt.Builder(logger)
network = builder.create_network()
parser = trt.OnnxParser(network, logger)

# 解析 ONNX
with open("model.onnx", "rb") as f:
    parser.parse(f.read())

# 构建引擎
engine = builder.build_cuda_engine(network)

# 推理
context = engine.create_execution_context()
context.execute(bindings=bindings)
```

---

## 26.4 边缘设备部署

### 26.4.1 Jetson Nano

```bash
# 安装 JetPack
sudo apt install nvidia-jetpack

# 运行模型
python3 infer.py --model model.onnx --device cuda
```

### 26.4.2 树莓派

```bash
# 安装 TensorFlow Lite
pip install tflite-runtime

# 运行 TFLite 模型
python3 infer.py --model model.tflite
```

---

## 26.5 本章小结

### 核心知识点

1. **模型压缩** - 剪枝、量化、蒸馏
2. **ONNX** - 通用格式
3. **TensorRT** - NVIDIA 优化
4. **边缘部署** - Jetson、树莓派

### 练习题目

1. 量化 ResNet 模型
2. 导出 ONNX 并部署
3. 部署到 Jetson Nano

### 下章预告

最后一章我们将展望**前沿技术**，探讨 CV 未来发展方向。

---

## 参考资料

1. [ONNX](https://onnx.ai/)
2. [TensorRT](https://developer.nvidia.com/tensorrt)
3. [OpenVINO](https://docs.openvino.ai/)

---

**更新日期：** 2026-03-12  
**作者：** OpenClaw
